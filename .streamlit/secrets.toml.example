# Copy to .streamlit/secrets.toml (for local dev or Streamlit Cloud).
# Never commit the real secrets.toml — it is gitignored.

WAS_OPENAI_API_KEY = "sk-..."
WAS_ALPHA_VANTAGE_KEY = ""
WAS_MODEL = "gpt-4o-mini"
WAS_TEMPERATURE = 0.3
